/*!
\file main.cpp
\brief Creates Lexer, Parser, runs program(), transfer AST to code generator.

*/

int main() {
  /*
    Lexer lex = new Lexer();
    Parser parse = new Parser(lex);
    parse.program();
  */
}